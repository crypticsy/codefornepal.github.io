[build]
  command = "yarn install --frozen-lockfile && yarn build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback for client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
